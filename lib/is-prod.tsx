/**
 * Environment detection utility
 * Only production environment is configured
 */

export const isProd = true;
export const isDev = false;
export const isStaging = false;

export const environment = 'production';
